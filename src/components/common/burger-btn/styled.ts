import styled from "styled-components";

export const StyledButton = styled.button`
    z-index: 100000;
    cursor: pointer;
    background-color: transparent;
    border-radius: 50%;
    &:hover{
        box-shadow: 4px 4px 40px 14px rgba(255, 255, 255, 0.2);
    }
`